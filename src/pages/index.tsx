import React from 'react';
import type { HeadFC, PageProps } from "gatsby"
import {pageStyles, headingStyles, headingAccentStyles, paragraphStyles, codeStyles, listStyles, doclistStyles, listItemStyles, linkStyle, docLinkStyle, descriptionStyle, badgeStyle} from '../styles/indexStyles';

const Index = () => {
  return (
    <main>
      <Header />
    </main>
  );
};

export default Index;
